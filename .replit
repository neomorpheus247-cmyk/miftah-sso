modules = ["php-8.2", "vue-node-20", "nodejs-20", "bash"]
[agent]
expertMode = true

[nix]
channel = "stable-25_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Laravel App"

[[workflows.workflow]]
name = "Laravel App"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "./start-app.sh"
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 40923
externalPort = 3000

[deployment]
deploymentTarget = "autoscale"
run = ["bash", "-c", "export APP_KEY='base64:+r1F3hqEyBH+FbV0bwVYuf21RYfS9c8mfC68G1Wg3ww=' && export DB_CONNECTION=sqlite && export DB_DATABASE=/tmp/database.sqlite && export APP_ENV=production && export APP_DEBUG=false && export CACHE_STORE=file && export SESSION_DRIVER=file && export QUEUE_CONNECTION=database && export SESSION_SECURE_COOKIE=false && export SESSION_DOMAIN=null && touch /tmp/database.sqlite && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=5000"]
build = ["npm", "run", "build"]
